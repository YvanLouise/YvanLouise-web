# Contributing

感谢你关注这个项目。

这个仓库首先是一个真实运行中的个人网站代码库，所以欢迎建设性的协作，但也希望所有改动都尽量清晰、克制并可验证。

## 适合贡献的内容
- 修复明确的 bug
- 提升可访问性、稳定性或文档质量
- 改进静态公开站的性能与体验
- 优化构建、部署或内容维护流程
- 清理明显的乱码、结构不一致或过时说明

## 不建议直接提交的内容
- 未经讨论的大规模视觉重做
- 强绑定外部付费平台的重构
- 带真实密钥、账号或生产配置的改动
- 访客私信、评论、上传媒体等私有数据
- 会影响公开站可用性的高风险破坏性修改

## 开始之前
1. Fork 或基于分支进行开发
2. 安装依赖：`npm install`
3. 按需复制环境变量模板
4. 在本地运行需要验证的站点

## 本地常用命令
```bash
npm run dev
npm run dev:visitor
npm run dev:developer
npm run build
npm run ci
```

提交前至少执行一次：
```bash
npm run build
```

## 仓库结构速览
- `public-site/`：公开访客站
- `admin-site/`：本地内容管理与实验性后台逻辑
- `shared/`：共享代码
- `content/`：公开内容数据文件
- `backend/`：历史本地后端与迁移工具
- `supabase/`：旧方案参考

## 提交建议
- 提交信息尽量聚焦单一目的
- 一个提交只解决一类问题更好 review
- 改文档时请同步更新相关说明，不要只改其中一处
- 如果改动影响部署或发布链路，请在描述里写清楚影响范围

## Pull Request 建议
PR 描述里最好包含：
- 改了什么
- 为什么要改
- 如何验证
- 是否影响公开站部署

如果是界面改动，附截图会很有帮助。

## 安全与隐私
向仓库提交代码前，请再次确认：
- 没有提交真实 `.env` 文件
- 没有提交日志、调试缓存和临时文件
- 没有提交访客私信、评论、上传资源或其他私有数据
- 没有把生产 Token、Cookie、管理员密码哈希或第三方平台密钥写进源码

## 许可证说明
向本仓库提交代码即表示你同意这些贡献在本项目的 MIT License 下发布。